{
  "name": "@vitbokisch/personal-web",
  "version": "0.0.0",
  "description": "A personal website",
  "main": "index.js",
  "repository": "https://github.com/vitbokisch/bokisch.com.git",
  "author": "Vit Bokisch <vit@bokisch.cz>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "next src",
    "build": "next build src",
    "export": "next export src",
    "start": "next start src",
    "pkgs": "rm -rf node_modules/ package-lock.json yarn.lock && yarn",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && cp CNAME out/CNAME && git add out/ && git commit -m \"Deploy new version to gh-pages\" && git subtree push --prefix out origin gh-pages",
    "pkgs:clean": "rm -rf node_modules/ yarn.lock && yarn",
    "pkgs:build": "lerna run build --stream"
  },
  "dependencies": {
    "@vitus-labs/coolgrid": "^0.10.0",
    "@vitus-labs/core": "^0.10.0",
    "@vitus-labs/elements": "^0.10.0",
    "@vitus-labs/rocketstyle": "^0.10.0",
    "@vitus-labs/unistyle": "^0.10.0",
    "mobx": "^6.1.5",
    "mobx-react": "^7.1.0",
    "mobx-state-tree": "^5.0.1",
    "next": "^10.0.5",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "styled-components": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.22",
    "@types/react": "^17.0.0",
    "@types/styled-components": "^5.1.7",
    "@vitus-labs/tools-babel": "^0.3.1",
    "@vitus-labs/tools-lint": "^0.3.1",
    "@vitus-labs/tools-typescript": "^0.3.0",
    "imagemin-optipng": "^8.0.0",
    "imagemin-svgo": "^8.0.0",
    "next-compose-plugins": "^2.2.1",
    "next-optimized-images": "^2.6.2",
    "typescript": "^4.1.3"
  }
}
