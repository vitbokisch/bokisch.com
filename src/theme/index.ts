import { Provider } from '@vitus-labs/rocketstyle'
import theme, { type Theme } from './theme'
import ThemeProvider from './ThemeProvider'

export type { Theme }
export { ThemeProvider, Provider, theme }
