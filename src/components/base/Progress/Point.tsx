import symbol from '../Symbol'

export default symbol
  .config({
    name: 'base/Progress/Point',
  })
  .theme({
    position: 'absolute',
    top: '50%',
    right: 0,
    transform: 'translateY(-50%)',
  })
